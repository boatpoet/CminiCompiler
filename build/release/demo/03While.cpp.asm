DSEG	SEGMENT
STORE	DW	4 DUP(?)
TEMP	DW	64 DUP(?)
DSEG	ENDS
SSEG	SEGMENT	STACK
FUNCST	DW	20 DUP(?)
SSEG	ENDS
CODE	SEGMENT
	ASSUME	CS:CODE
DISPLAY	PROC	FAR
MOV	CX,0
DISP1:	MOV	DX,0
DIV	BX
PUSH	DX
INC	CX
OR	AX,AX
JNE	DISP1
DISP2:	MOV	AH,02H
POP	DX
ADD	DL,30H
INT	21H
LOOP	DISP2
RET
DISPLAY	ENDP
CODE	ENDS
CSEG	SEGMENT
	ASSUME	CS:CSEG,DS:DSEG,SS:SSEG
START:
MOV	AX,DSEG
MOV	DS,AX
MOV	AX,SSEG
MOV	SS,AX
MOV	SI,OFFSET STORE
MOV	DI,OFFSET TEMP
MOV	SP,SIZE FUNCST
MOV	DX,10
MOV	[SI+0],DX
MOV	DX,0
MOV	[SI+2],DX
WH1:
MOV	DX,[SI+0]
MOV	BX,0
CMP	DX,BX
JA	DX11
JBE	DX01
DX11:
MOV	DX, 01H
JMP	CONTINUE1
DX01:
MOV	DX, 00H
JMP	CONTINUE1
CONTINUE1:
MOV	[DI+0],DX
MOV	DX,[DI+0]
CMP	DX,01H
JE	DO1
JNE	WHEND1
DO1:
MOV	DX,[SI+2]
MOV	BX,[SI+0]
ADD	DX,BX
MOV	[SI+2],DX
MOV	DX,[SI+0]
MOV	BX,1
SUB	DX,BX
MOV	[SI+0],DX
JMP	WH1
WHEND1:
MOV	BX,10
MOV	AX,[SI+2]
CALL	FAR PTR	DISPLAY
MOV	AH,4CH
INT	21H
CSEG	ENDS
	END	START
