DSEG	SEGMENT
STORE	DW	6 DUP(?)
TEMP	DW	64 DUP(?)
DSEG	ENDS
SSEG	SEGMENT	STACK
FUNCST	DW	20 DUP(?)
SSEG	ENDS
CODE	SEGMENT
	ASSUME	CS:CODE
DISPLAY	PROC	FAR
MOV	CX,0
DISP1:	MOV	DX,0
DIV	BX
PUSH	DX
INC	CX
OR	AX,AX
JNE	DISP1
DISP2:	MOV	AH,02H
POP	DX
ADD	DL,30H
INT	21H
LOOP	DISP2
RET
DISPLAY	ENDP
MOV	DX,5
MOV	[SI+0],DX
max	PROC	FAR
MOV	DX,[SI+0]
MOV	BX,[SI+2]
ADD	DX,BX
MOV	[SI+0],DX
MOV	DX,[SI+2]
RET
max	ENDP
CODE	ENDS
CSEG	SEGMENT
	ASSUME	CS:CSEG,DS:DSEG,SS:SSEG
START:
MOV	AX,DSEG
MOV	DS,AX
MOV	AX,SSEG
MOV	SS,AX
MOV	SI,OFFSET STORE
MOV	DI,OFFSET TEMP
MOV	SP,SIZE FUNCST
MOV	DX,3
MOV	[SI+0],DX
MOV	DX,5
MOV	[SI+2],DX
CALL	FAR PTR	max
MOV	[SI+4],DX
MOV	BX,10
MOV	AX,[SI+4]
CALL	FAR PTR	DISPLAY
MOV	BX,10
MOV	AX,[SI+0]
CALL	FAR PTR	DISPLAY
MOV	AH,4CH
INT	21H
CSEG	ENDS
	END	START
